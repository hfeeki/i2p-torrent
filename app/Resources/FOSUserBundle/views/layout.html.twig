{% extends '::base.html.twig' %}
{% from 'MopaBootstrapBundle::flash.html.twig' import session_flash %}

{% block body %}
{% spaceless %}
{% block topbar %}
<div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
        <div class="container">
            <a class="brand" href="{{ path('homepage') }}" title="Unknown">Unknown</a>

            {{ knp_menu_render('SOTBCoreBundle:Builder:mainMenu', {'currentClass': 'active', 'ancestorClass': 'active'}) }}

            {% if is_granted("IS_AUTHENTICATED_FULLY") %}
            <div class="pull-right">
                <ul class="nav">
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Logged in as {{ app.user.username }} <b class="caret"></b></a>
                        <ul class="dropdown-menu">
                            <li class="divider"></li>
                            <li><a href="{{ path('fos_user_security_logout') }}">Sign Out</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
            {% endif %}

        </div>
    </div>
</div>
{% endblock topbar %}

<div class="container">
    <div class="content">
        {% block page_header %}
        <div class="page-header">
            <h1>{% block headline %}{% endblock headline %}</h1>
        </div>
        {% endblock page_header %}

        {% block flashes %}
        {{ session_flash() }}
        {% endblock flashes %}

        {% block content %}
        {% endblock content %}
    </div>
    <footer class="footer">
        {% block footer %}
        <p>&copy; <a href="http://www.drollette.com/" target="_blank">Matt Drollette</a> 2012</p>
        {% endblock footer %}
    </footer>
</div>
<!-- /container -->
{% endspaceless %}
{% endblock body %}